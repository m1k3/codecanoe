%h2 Marketing page with some wery nice marketing copy and images

- if user_signed_in?
  Logged in as:
  = current_user.email
  = link_to 'Go to my applications', apps_path
  = link_to 'Sign Out', destroy_user_session_path, data: {method: :delete}

- else
  = link_to 'Sign Up', new_user_registration_path
  = link_to 'Sign In', new_user_session_path
